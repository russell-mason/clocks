/**
 * Specifies the allowed icon types for an SVG button.
 * Values here represent the ID of the SVG in the app-svg-store component.
 */
export type Icon = 'next' | 'play' | 'stop' | 'check' | 'replay' | 'options' | 'instructions';
